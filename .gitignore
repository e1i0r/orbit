/orbit
/dist/
*.out
.DS_Store
.idea/
.vscode/

# Working notes: the design, the plans and the session handoff are kept
# alongside the code but are not published with it.
/docs/
/NEXT.md

# Agent worktrees: a full second copy of this module, created by the toolchain
# that drives development. It is never part of the module and never committed.
.claude/worktrees/
